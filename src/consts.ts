// Site-wide configuration — single source of truth for values that change per client.

// Google Calendar "Appointment Scheduling" booking link.
// Create it in Google Calendar → Create → "Appointment schedule" → Share → copy the
// public booking page link (looks like https://calendar.app.google/XXXXXXXX), then
// paste it here. Every "Book a free consultation" button uses this value.
export const BOOKING_URL = 'https://calendar.app.google/KKsMbY5DqjJRA7FD9';

export const PHONE_DISPLAY = '(214) 555-0123';
export const PHONE_HREF = 'tel:+12145550123';
